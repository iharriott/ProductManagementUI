import { DataFormatterPipe } from './data-formatter.pipe';

describe('DataFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new DataFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
