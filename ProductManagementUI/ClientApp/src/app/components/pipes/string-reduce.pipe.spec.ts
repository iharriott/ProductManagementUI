import { StringReducePipe } from './string-reduce.pipe';

describe('StringReducePipe', () => {
  it('create an instance', () => {
    const pipe = new StringReducePipe();
    expect(pipe).toBeTruthy();
  });
});
